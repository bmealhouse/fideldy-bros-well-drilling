import * as React from "react";
import { Box } from "@chakra-ui/core";

export default (props) => (
  <Box {...props}>
    Contact Fideldy Bros Well Drilling 218-999-0106 for all your Northern
    Minnesota well drilling service needs.
  </Box>
);
